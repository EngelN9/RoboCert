/-
Quarantine root for statement-only formalizations (Phase 0.5c+).

`sorry` is permitted in this library and NOWHERE else. It is never imported by `RoboCert`
and is not covered by the axiom audit. See `formal/AGENTS.md`.
-/
